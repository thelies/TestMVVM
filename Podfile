# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TestMVVM' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestMVVM
  pod 'RxSwift'
  pod 'RxDataSources'
  pod 'RxRealm'

  target 'TestMVVMTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestMVVMUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
